import AppShell from "@/components/AppShell";

export default function ProfielPage() {
  return (
    <AppShell title="Profiel">
      <div style={{ background: "white", border: "1px solid #e6e6e6", borderRadius: 16, padding: 16 }}>
        <h2 style={{ marginTop: 0 }}>Gegevens aanpassen</h2>
        <p>Hier komt straks een formulier om je registratiegegevens te wijzigen.</p>
      </div>
    </AppShell>
  );
}
